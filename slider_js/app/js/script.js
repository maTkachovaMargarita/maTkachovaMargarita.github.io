$(document).ready(function(){
	 $('.slider').slick(
	 // {
	 // 	dots: true;
	 // }
	 );

	 // $('.lazy').each(function(){
	 // const imgSrc =	$(this).attr('src');
	 // $(this).attr('data-lazy', $(this).attr('src'));
	 // });
});